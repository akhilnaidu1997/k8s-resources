apiVersion: v1
kind: Pod
metadata:
  name: resources
  namespace: roboshop
spec:
  containers:
  - name: nginx
    image: nginx
    resources:
      requests:
        cpu: "100m"
        memory: "250Mi" 
      limits:
        cpu: "500m" 
        memory: "500Mi" 
    ports:
    - containerPort: 80